---
description: Общие правила по проекту/коду
alwaysApply: true
---

#Стек
React (на Vite) - для фронтенда

Zustand - для управления глобальными состояниями на фронтенде (ну типо глобальные переменные) - гораздо проще аналогов и встроенного React Context, и прям предсказуемая, для такого уровня кайф

ExpressJS - для бэкенда простейшая и при этом достаточно эффективная библиотека для бэкенда на JS - +используем JS на бэкенде и на фронтенде, не распыляемся между двумя языками, поэтому предпочла JS, а не питон или с# условный

MySQL - бд, которую пропагандировали нам и скорее всего примут лучше чем пострес 

Prisma - ORM, для общения с БД, не с помощью голых SQL запросов, а с помощью готовых JS функций - и проще, и оптимизированнее, и современнее, но будь готова объяснить какая команда что значит, чтоб если доебались то непроебаться. Там все логично findMany - найти всех (SELECT тот же самый), ну и типо такого, догадаться легко

Без кэширования с помощью Redis - доебутся, попросят объяснить и излишне

Авторизация на JWT токенах - стандарт